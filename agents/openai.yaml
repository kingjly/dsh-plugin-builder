interface:
  display_name: "DSH Plugin Builder"
  short_description: "把一项能力做成可安装的 DeepSeek Harness 插件"
  default_prompt: "用 dsh-plugin-builder 做 dsh 插件：先判定形态（工具/钩子/提供方/UI），再生成树外 ESM 包、cordis.patch.yml，并给出 --patch 与 dsh plugin add 验证步骤。"
policy:
  allow_implicit_invocation: true
